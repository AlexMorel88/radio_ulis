<div class="section-playlists">
  <div class="tab__content">
    <section class="switchable ">
      <div class="container">
        <div class="row">
         <h2>Ulis de Lastvader</h2>
         <span h3><%= @playlists.count %> Playlists à découvrir </span>
         <span h3>- <%= Sound.all.count %> Sons à écouter </span>
         <div class="col switchable__text">
          <ul class="accordion accordion-1" style="min-height: 0px;">

            <% @playlists.each do |play| %>


            <li>
              <div class="accordion__title">
                <span class="h4"><%= play.title %> </span> - <span class="h5"><%= play.playlist_sound.count %> Émissions</span>
              </div>
              <% play.playlist_sound.each do |play_list| %>
              <div class="accordion__content">
                <div class="section-sounds">
                <div class="section-sound">
                  <h3><%= Sound.find(play_list.sound_id).title %></h3>
                  <div class="img-sound">
                    <img src="https://res.cloudinary.com/dvhymcfm3/image/upload/v1569358661/radio_ulis/poster/product-small-7.jpg"></img>
                  </div>
                  <div class="audio-sound">
                    <audio controls="true" controlsList="nodownload" src="<%= url_audio(Sound.find(play_list.sound_id)) %>" type="audio/mpeg">
                    </audio>
                  </div>
                </div>
              </div>

              </div>
              <% end %>
            </li>
            <% end %>

          </ul>

        </div>

      </div>

    </div>

  </section>
</div>
</div>