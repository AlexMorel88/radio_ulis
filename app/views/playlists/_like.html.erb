<%= form_with scope: :sound, url: sound_path(sound), method: :put, remote: false do |f| %>
    
 
        <%= f.hidden_field :click_like, value: true %>
      

     <%= f.submit "", :type => :image, :src => asset_path("like.svg"), class:'like-btn-js' %>


  <% end %>